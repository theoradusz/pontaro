baseURL = "https://example.netlify.app/"  # Will be auto-set by Netlify
languageCode = "en-us"
title = "My Blog"
theme = "ananke"

[params]
  description = "A simple blog"
  show_reading_time = true

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
