[build]
  command = "hugo"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.141.0"

# Redirect /admin to /admin/ (trailing slash)
[[redirects]]
  from = "/admin"
  to = "/admin/"
  status = 301
